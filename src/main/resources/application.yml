spring:
  application:
    name: tickethub

  datasource:
    url: jdbc:postgresql://${POSTGRES_URL}/tickethub
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
